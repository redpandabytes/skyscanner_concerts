/// <reference types="es6-promise" />
/// <reference types="node" />
import { IncomingMessage } from "http";
import App from "./app";
export default class Permissions {
    private app;
    anonymous: UserPermissions;
    authenticated: UserPermissions;
    constructor(app: App);
    user(userId: string): UserPermissions;
    feed(feedName: string): FeedPermissions;
}
export declare class UserPermissions {
    userId: string;
    private app;
    private predefinedUserType;
    constructor(userId: string, app: App, predefinedUserType: boolean);
    get(): Promise<IncomingMessage>;
    add(options: any): Promise<any>;
    remove(options: any): Promise<any>;
    private updatePermissions(operation, options);
}
export declare class FeedPermissions {
    feedName: string;
    private app;
    constructor(feedName: string, app: App);
    get(): Promise<IncomingMessage>;
    add(options: any): Promise<any>;
    remove(options: any): Promise<any>;
    private updatePermissions(operation, options);
}
