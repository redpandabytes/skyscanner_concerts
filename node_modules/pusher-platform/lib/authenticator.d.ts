/// <reference types="node" />
import { IncomingMessage, ServerResponse } from "http";
import { AuthenticateOptions } from "./common";
export default class Authenticator {
    private appId;
    private appKeyId;
    private appKeySecret;
    constructor(appId: string, appKeyId: string, appKeySecret: string);
    authenticate(request: IncomingMessage, response: ServerResponse, options: AuthenticateOptions): void;
    private authenticateWithClientCredentials(response, options);
    private authenticateWithRefreshToken(oldRefreshToken, response, options);
    private generateAccessToken(options);
    private generateRefreshToken(options);
}
