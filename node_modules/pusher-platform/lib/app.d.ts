/// <reference types="es6-promise" />
/// <reference types="node" />
import { IncomingMessage, ServerResponse } from "http";
import BaseClient from "./base_client";
import { AuthenticateOptions, RequestOptions } from "./common";
export interface Options {
    cluster: string;
    appId: string;
    appKey: string;
    client?: BaseClient;
}
export default class App {
    private client;
    private appId;
    private appKeyId;
    private appKeySecret;
    private authenticator;
    constructor(options: Options);
    request(options: RequestOptions): Promise<IncomingMessage>;
    configRequest(options: RequestOptions): Promise<IncomingMessage>;
    authenticate(request: IncomingMessage, response: ServerResponse, options: AuthenticateOptions): void;
    private scopeRequestOptions(prefix, options);
    private generateSuperuserJWT();
}
